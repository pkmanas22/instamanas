/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['platform-lookaside.fbsbx.com', 'avatars.githubusercontent.com', 'instamanas.s3.ap-south-1.amazonaws.com'],
    },
};

export default nextConfig;

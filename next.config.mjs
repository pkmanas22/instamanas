/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['platform-lookaside.fbsbx.com', 'manaskpradhan.vercel.app', 'instamanas.s3.ap-south-1.amazonaws.com'],
    },
};

export default nextConfig;
